apiVersion: apps/v1
kind: Deployment
metadata:
  name: '{{.LabID}}-deployment'
  namespace: devsarena
  labels:
    app: '{{.LabID}}'
    language: '{{.Language}}'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: '{{.LabID}}'
  template:
    metadata:
      labels:
        app: '{{.LabID}}'
      annotations:
        container.apparmor.security.beta.kubernetes.io/app-container: runtime/default
    spec:
      runtimeClassName: gvisor  
      volumes:
        - name: workspace-volume
          emptyDir:
            sizeLimit: 2Gi
      securityContext:
        appArmorProfile:
          type: RuntimeDefault
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: "OnRootMismatch"
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: copy-r2-content
          image: amazon/aws-cli:latest
          command:
            - /bin/sh
            - -c
            - |
              echo 'R2 bucket: {{.S3Bucket}}' && echo 'R2 key: {{.S3Key}}' && aws s3 cp s3://{{.S3Bucket}}/{{.S3Key}} /workspace --recursive --endpoint-url https://$R2_ACCOUNT_ID.r2.cloudflarestorage.com
          env:
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: R2_ACCESS_KEY
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: R2_SECRET_KEY
            - name: AWS_DEFAULT_REGION
              value: "auto" # R2 uses 'auto' region
            - name: R2_ACCOUNT_ID
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: R2_ACCOUNT_ID
          volumeMounts:
            - name: workspace-volume
              mountPath: /workspace
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
{{- if .RequiresInitCommand }}
        - name: setup-init
          image: krishnawyvern/devsarena-node-runtime:v1.1
          command:
            - /bin/sh
            - -c
            - |
              echo "Running setup command: {{.RequiresInitCommand}}"
              cd /workspace && {{.RequiresInitCommand}}
          volumeMounts:
            - name: workspace-volume
              mountPath: /workspace
          workingDir: /workspace
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
              add:
                - CHOWN
                - DAC_OVERRIDE
                - SETUID
                - SETGID
{{- end }}

      containers:
        - name: app-container
          image: krishnawyvern/devsarena-node-runtime:v1.1
          ports:
            - name: user-app-vite
              containerPort: 5173
            - name: user-app-serve
              containerPort: 3000
            - name: pty-ws
              containerPort: 8082
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
              add:
                - CHOWN
                - DAC_OVERRIDE
                - SETUID
                - SETGID
          env:
            - name: REDIS_URI
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: REDIS_URI
            - name: LAB_ID
              value: '{{.LabID}}'
            - name: SECURITY_MODE
              value: "restricted"
            - name: PTY_PING_INTERVAL
              value: "30"
            - name: PTY_PONG_TIMEOUT
              value: "60"
          volumeMounts:
            - name: workspace-volume
              mountPath: /workspace
          workingDir: /workspace

        - name: runner-container
          image: krishnawyvern/devsarena-runner-service
          ports:
            - name: fs-ws # WebSocket for file system events
              containerPort: 8081
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
              add:
                - CHOWN
                - DAC_OVERRIDE
          env:
            - name: LAB_ID
              value: '{{.LabID}}'
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: R2_ACCESS_KEY
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: R2_SECRET_KEY
            - name: AWS_DEFAULT_REGION
              value: "auto" # R2 uses 'auto' region
            - name: AWS_S3_BUCKET_NAME
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: AWS_S3_BUCKET_NAME
            - name: WORKSPACE_DIR
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: WORKSPACE_DIR
            - name: REDIS_URI
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: REDIS_URI
            - name: R2_ACCOUNT_ID
              valueFrom:
                secretKeyRef:
                  name: aws-secrets
                  key: R2_ACCOUNT_ID
          volumeMounts:
            - name: workspace-volume
              mountPath: /workspace
          workingDir: /workspace
